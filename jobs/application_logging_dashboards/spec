---
name: application_logging_dashboards

description: |
  This job was created to be deployed next to grafana job from prometheus-boshrelease
  These dashboards add cluster capabilities to group the application log stats and costs metrices



packages: []

# DO NOT use the same names as other dashboards (from other jobs)
# otherwise they could be overwriten
templates:
  app_log_cost.json: app_log_cost.json
  app_logging_stats.json: app_logging_stats.json
  
properties: {}
